---
name: coding-vps-integration-trae-antigravity-2026-07-08
description: >
  Squad 5: MCP orchestrator real state is 62 tools (not 100). TRAE/Antigravity/Cursor
  configs must point to scripts/coding_vps_mcp_orchestrator.py mcp. redis_ping was
  implemented but unregistered; health_check_all added as thin list_services wrapper.
  Never commit LITELLM keys in mcp_config JSON.
type: project
date: 2026-07-08
agent: squad5-integration
priority: P1
status: active
---

# Memory — coding-vps MCP Integration (TRAE / Antigravity) 2026-07-08

## Fato central (não errar de novo)

| Claim em docs antigas | Realidade CLI (`python … list` / `health_check_all`) |
|-----------------------|----------------------------------|
| 100 tools / 15 categories | **62 tools / 13 categories** (pós Squad 10 dedupe + Squad 5 aliases) |
| 92 / 85 tools | stale |
| 51/89 services UP | **26/80 up** no snapshot Squad 5 (`health_check_all stack=all`) |
| 4 MCP tools only | stale (skill antiga) |

Fonte de verdade: `scripts/coding_vps_mcp_orchestrator.py` docstring + `list`.

## Padrão de integração MCP

```text
command = Python absoluto (Frameworks 3.14 no Mac Gustavo)
args    = [<repo>/scripts/coding_vps_mcp_orchestrator.py, "mcp"]
env     = SSH_PRIVATE_KEY, SSH_TAILSCALE_HOST
# LITELLM_API_KEY: só em config LOCAL / secrets file — nunca no git
```

Templates versionados:
- `scripts/mcp_config.trae.json`
- `scripts/mcp_config.antigravity.json`
- `scripts/mcp_config.cursor.json`
- `scripts/mcp_config.claude_desktop.json`
- Doc: `docs/platforms/coding-vps/INTEGRATION_TRAE_ANTIGRAVITY.md`

Install: `bash scripts/install_mcp_clients.sh install`

## Easy wins aplicados (Squad 5)

1. **`redis_ping`** — função já existia (`_redis_cmd` + PONG check); **não estava em `_register_db()`**. Registrada.
2. **`health_check_all`** — wrapper fino de `list_services` (summary up/down, sem TCP por serviço).
3. Contagem: 60 → **62** (não re-inflar para 100).

## Anti-padrões

1. **Docs com contagem de tools inventada** — sempre rodar `list` antes de documentar.
2. **Secrets em JSON de MCP no repo** — keys `LITELLM_*` / `sk-*` em `mcp_config.*.json` são vazamento. Usar env local.
3. **Função implementada ≠ tool registrada** — grepar `_register_*` e `list` juntos.
4. **Assumir 89 services UP** — `list_services` / `health_check_all` retornam total real; EasyPanel UI ≠ Swarm.

## Validação

```bash
bash scripts/validate_coding_vps_tools_60.sh          # SSH-backed smoke
bash scripts/validate_coding_vps_tools_60.sh --quick  # só list/openapi
```

Exit ≠ 0 se count < 62 ou tools obrigatórias ausentes.

## Relação com lesson 158

Lesson 158 falava de 26 services / MiniMax já ativo. Continua válida para **infra**.
Esta lesson cobre **MCP client integration + tool catalog honesty** (Squad 5).

## Referências

- `.agents/skills/coding-vps-tools-100/SKILL.md` — status real atualizado
- `.harness/memory/lesson-158-coding-vps-real-state-2026-07-08.md` — estado VPS
- `scripts/MCP_USAGE.md` — protocolo

Modified by Gustavo Almeida
