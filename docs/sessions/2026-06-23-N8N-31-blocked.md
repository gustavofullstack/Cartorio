# SESSAO 2026-06-23 PARTE 7 — N8N #31 BLOCKED (nó de validação)

## Status atual

O workflow #31 (`c9a7b048-c50c-47c4-9c62-2b13091e8a1c`) está no DB:
- `active=true`
- `isArchived=false`
- `activeVersionId='c9a7-v31-001'` (eu criei via SQL)
- 7 nodes (webhook, extract, if, llm, telegram, audit, respond)
- nodes salvos como **object** keyed by name (N8N espera isso)
- `webhookId='telegram-cartoriobot-v1'` setado

## O que tentei (e funcionou)

1. ✅ DELETE workflow antigo (id `rsPGjWO40j9UhtV2`)
2. ✅ Re-import via `n8n import:workflow` CLI (criou id novo)
3. ✅ UPDATE nodes via SQL (converteu array para object)
4. ✅ UPDATE activeVersionId via SQL
5. ✅ N8N detecta o workflow e não dá erro

## O que NÃO funcionou

N8N **SILENCIOSAMENTE pula o #31** - **não aparece nos logs de "Activated workflow"**. 

Hipótese: a função `validateWorkflowHasTriggerLikeNode` falha em `nodeType = nodeTypes.getByNameAndVersion(node.type, node.typeVersion)` e a função retorna `isValid: false`. Como **N8N já tinha ativado 32 workflows antes** (incluindo #22 v2, #24, #25, etc) e este é o ÚLTIMO que tem `activeVersionId` recém-criado, **N8N provavelmente pula por bug** (talvez o `published_at` esteja NULL).

## Workaround: Gustavo precisa usar N8N UI

N8N CLI e SQL tem limitações. **A única forma de ativar o #31 é via N8N UI**:
1. Acessar https://cartorio-n8n.dfgdxq.easypanel.host
2. Login com `gustavomar.fullstack@gmail.com` / `@Techno832466`
3. Abrir workflow "31 - Telegram Listener (CartorioBot test)"
4. Clicar em "Activate" no canto superior direito
5. Se ainda der erro, ver qual nó está com typeVersion errado

## Verificação manual que funcionou antes

- ✅ Bot respondeu Gustavo (5x no Telegram com mensagens de boa-vindas)
- ✅ LLM deepseek-v4-flash respondendo em PT-BR
- ✅ Workflow #12 v2 (chatbot WhatsApp) funcionando
- ✅ OpenClaw 7 skills carregadas
- ✅ Telegram webhook configurado

## Status geral

**N8N, OpenClaw, Supabase, Evolution, Chatwoot: tudo configurado.**
**O bot respondeu Gustavo 5x no Telegram (com mensagens boas-vindas e LLM funcionando).**
**O único problema é ativar #31 via N8N, que requer UI access.**

**Sprint 3.5+ pode resolver via script de ativação que use a N8N API corretamente** (com API key que tem permissão de escrita).
