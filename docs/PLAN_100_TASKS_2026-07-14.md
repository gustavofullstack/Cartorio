# SUPER PLANO 100 T — Auditoria Cartório (snapshot 2026-07-14)

> Plano derivado de evidência real (HTTP, Swarm, logs, Git, pytest, ruff, mypy, cobertura, DNS).
> Squads: **S0 — Backend Hardening (cartorio-dev)** · **S1 — N8N & Multi-canal (cartorio-n8n)** ·
> **S2 — LGPD & Segurança (cartorio-lgpd)** · **S3 — Plataformas (cartorio-sre)**.
> Concorrência: **máx. 2 sub-agentes simultâneos** (regra `prompt-cartorio`).
> Cada bloco traz ID, comando, validação e evidência esperada.

## Estado de partida (snapshot 2026-07-14T18:15Z)

- API: 200, versão reportada `0.6.0` (OpenAPI e `/health`) mas `app/main.py:336` = `0.5.4` e `mcp_server.py:52` = `0.4.0` (drift).
- ruff `0`, mypy `0` (128 arquivos), pytest `2625 passed`, cobertura `95,09%` (gate ≥90% PASS).
- Falha única: `test_mutation_gate::test_mutmut_installed_version` (mutmut não instalado no venv 3.12). Não regressão.
- VPS via SSH público: `cartorio_api 11/1` (10 réplicas velhas + 1 nova), `chatwoot 0/1`, `chatwoot-sidekiq 1/1`,
  `evolution-api 0/1`, `n8n 1/1`, `openclaw-gateway 1/1`, `lobechat 1/1`, `redis 1/1`, `supabase 1/1`,
  `coding-vps_apenas_para_auxilio_mcp-orchestrator 1/1` (risco de tenancy cross-stack).
- DNS: `lobe`, `chatwoot`, `n8n`, `supabase.2notasudi.com.br` NXDOMAIN. Demais 7 OK.
- Tailscale: `Logged out.`
- Telegram: bot `pietra_ceo_bot`, webhook URL configurada, `pending=0`.
- Logs-fonte do incidente: `chatwoot` e `evolution-api` tentando `10.11.211.12:5432` (IP público antigo).
  `n8n` aponta `cartorio_supabase` correto, mas autenticação falha (credenciais).
- Memória: 173 lessons cross-rein; 459 checkboxes Harness, 140 done (30,5%) — divergente do "85% global".

---

## Squad S0 — Backend Hardening (cartorio-dev) · T001–T025

| T | Comando / ação | Validação | Evidência |
|---|----------------|-----------|-----------|
| T001 | `git diff` version drift | `grep -n "version" backend/app/main.py mcp_server.py` retorna 0 divergência entre 0.5.4/0.6.0/0.4.0 | `make test` continua 2625 passed |
| T002 | `docker service update --image easypanel/cartorio/api:latest cartorio_api` após corrigir 11→1 réplicas | `docker service ls` mostra `cartorio_api 1/1` | `/api/v1/health/radar` online |
| T003 | Eliminar `verify=False` em `telegram.py:315,2213` | `ruff` 0 + `tests/test_telegram.py` 100% | Sem `InsecureRequestWarning` |
| T004 | Adicionar `mutmut` ao `pyproject.toml` deps dev e fixar `test_mutation_gate` para checar versão ≥3.6.0 | `uv pip install -e ".[dev]"` + `pytest -k mutmut` | `0 failed` |
| T005 | Criar `tests/test_openapi_contract.py` snapshot de paths OpenAPI | pytest novo com 105 métodos, 103 paths | arquivo `tests/test_openapi_contract.py` |
| T006 | Adicionar `make test-loop` que repete o suite até 3x para confirmar estabilidade (não flaky) | `make test-loop` → 3 execuções com mesmo nº de passed | saída Makefile |
| T007 | Refatorar `cartorio_api` Swarm replicas `11/1` para `1/1` (decisão de scale-out) | `docker service scale cartorio_api=1` | Swarm list mostra `1/1` |
| T008 | Bloquear reload de produção: `Dockerfile:80` sem `--reload` | `grep -v reload Dockerfile` | CI local |
| T009 | Hardening: `TrustedHostMiddleware` para hosts oficiais | `pytest tests/test_main.py -k host` | Log mostra bloqueio |
| T010 | Padronizar `app.version` via `pyproject.toml` (`__version__`) | `python -c "import app; print(app.__version__)"` | print consistente |
| T011 | Adicionar `httpx.AsyncClient` com `verify=True` por padrão | `tests/test_http.py` | sem `InsecureRequestWarning` |
| T012 | Adicionar testes de contrato para `/health`, `/ready`, `/api/v1/health/radar` (200 + JSON schema) | `pytest tests/test_health.py` | 100% verde |
| T013 | Adicionar `POST /api/v1/health/radar` admin-only para força refresh | `tests/test_radar.py::test_force_refresh` | 200 + `force_refreshed=true` |
| T014 | Migrar `docker_service_update` para função `app.services.swarm_ops` (testável sem Docker) | `pytest tests/test_swarm_ops.py` | mock-based, 0 chamadas reais |
| T015 | Adicionar `make audit-secrets` que bloqueia padrões `sk-*`, `ghp_*`, `AIza*` no repo | `make audit-secrets` exit 0 | 0 hits no repo |
| T016 | Substituir 27 secrets hardcoded em `mutants/tests` por `monkeypatch.setenv` | `ruff check` 0 + `pytest tests/mutants` 0 falhas | mutants passam sem segredos |
| T017 | Adicionar `tests/test_postgres_pool.py` exercitando `DB_POOL_SIZE=25` (SQUAD A21) | pytest verde | 25 conexões simultâneas |
| T018 | Criar `scripts/dump_metrics.py` (CLI) que exibe `cartorio_*` métricas atuais | `python scripts/dump_metrics.py` | 30+ métricas listadas |
| T019 | Padronizar CORS allowlist em 1 arquivo (`backend/app/config.py`) | grep `CORS_ORIGINS` retorna 1 fonte | CORS test passa |
| T020 | Adicionar `tests/test_mcp_auth.py` garantindo 401 sem header | pytest verde | rejeição sem token |
| T021 | Implementar fallback de versão OpenAPI no `mcp_server.py:52` lendo de `pyproject.toml` | `python -c "import mcp_server; print(mcp_server.__version__)"` | 0.5.4/0.6.0 unificado |
| T022 | Bloquear `OPENAI_API_KEY=sk-xxxx` placeholder em `lobechat` env | `docker service inspect cartorio_lobechat` | env validado |
| T023 | Adicionar `tests/test_db_pool_timeout.py` (excede pool, recebe exception) | pytest verde | erro 503 explicito |
| T024 | Padronizar `model_validate` vs `parse_obj` em routers | `ruff check --select UP` | 0 hits |
| T025 | Adicionar benchmark `pytest-benchmark` para `/api/v1/emolumento/calcular` | `make bench-emolumento` | P99 < 30ms |

## Squad S1 — N8N & Multi-canal (cartorio-n8n) · T026–T050

| T | Comando / ação | Validação | Evidência |
|---|----------------|-----------|-----------|
| T026 | `docker service update --env-add POSTGRES_HOST=cartorio_supabase --env-rm POSTGRES_HOST=10.11.211.12 cartorio_chatwoot` | `docker service inspect cartorio_chatwoot --format '{{.Spec.TaskTemplate.ContainerSpec.Env}}'` | mostra `cartorio_supabase` |
| T027 | Idem T026 para `cartorio_evolution-api` (`DATABASE_PROVIDER=postgresql`, `DATABASE_URL=postgres://supabase_admin:***@cartorio_supabase:5432/evolution?sslmode=disable`) | env inspecionado | sem 10.11.211.12 |
| T028 | Idem T026 para `cartorio_n8n` (atualizar `DB_POSTGRESDB_PASSWORD` para `@Techno832466`) | `docker service logs cartorio_n8n` | sem "password authentication failed" |
| T029 | Reconciliar `ENABLE_ACCOUNT_SIGNUP=false` em `cartorio_chatwoot` e `chatwoot-sidekiq` (Sidekiq está `true`) | `docker service inspect` ambos | `false` em ambos |
| T030 | `docker service scale cartorio_evolution-api=1` após fix de env | Swarm list | `1/1` |
| T031 | Reativar WhatsApp TriQ Hub via Easypanel UI (Gustavo) | `wget -qO- http://cartorio_evolution-api.1.*:8080/instance/connectionState/cartorio-2notas` | `state=open` |
| T032 | Criar A record `chatwoot.2notasudi.com.br -> 187.77.236.77` no Cloudflare (UI Gustavo) | `dig +short chatwoot.2notasudi.com.br` | `187.77.236.77` |
| T033 | Idem T032 para `n8n.2notasudi.com.br` (proxy OFF) | dig | `187.77.236.77` |
| T034 | Idem T032 para `supabase.2notasudi.com.br` | dig | `187.77.236.77` |
| T035 | Idem T032 para `lobe.2notasudi.com.br` (proxy OFF) | dig | `187.77.236.77` |
| T036 | Configurar OpenClaw gateway password no Control UI (Gustavo) | `wget -qO- http://cartorio_openclaw-gateway.1.*:18790/health` | 200 |
| T037 | Exportar todos workflows N8N para `infra/n8n-workflows/` | `make n8n-export` | 35 JSONs |
| T038 | Provisionar MCP server (n8n) com auth correto (`N8N_MCP_URL` + `MCP_API_KEY`) | `curl -X POST https://flow.2notasudi.com.br/mcp-server/http` | `200` ou `tools/list` |
| T039 | Configurar webhook Telegram `/telegram` e `webhook/chatbot-llm` com `X-API-Key` | logs webhook | `ok=true` |
| T040 | Adicionar `chatwoot_n8n_integration` test E2E | `pytest -m e2e tests/test_n8n_chatwoot.py` | 0 falhas |
| T041 | Padronizar `wf_*` prefix nos IDs de workflow N8N | API `/api/v1/workflows` | IDs consistentes |
| T042 | Provisionar alertmanager → Telegram para SLO breaker (1% wf failure) | alarme chega no chat | 1 alerta sintético |
| T043 | Habilitar `n8n-nodes-mcp` em WF12 (chatbot-llm) | execução WF12 | 200 |
| T044 | Adicionar `tests/test_n8n_health.py` que valida `/healthz` e `/api/v1/workflows` | pytest | 0 falhas |
| T045 | Validar webhook `/agendar-atendimento` (path real) | curl smoke | 200 |
| T046 | Validar webhook `/evo-in` (Evolution → N8N) | curl com payload Evolution | 200 + log |
| T047 | Provisionar `n8n-nodes-chatwoot` em WF03 (handoff-human) | execução WF03 | handoff log |
| T048 | Adicionar rota Traefik para `lobe.2notasudi.com.br` → `cartorio_lobechat:3210` | `dig +short lobe.2notasudi.com.br` + curl | 200 + chatwoot não interfere |
| T049 | Revalidar `Webhook routing` no EasyPanel (label `easypanel-cartorio`) | 6 subdomínios OK | `/dns-check` 200 |
| T050 | Bloquear container `coding-vps_apenas_para_auxilio_mcp-orchestrator` no mesmo Swarm do cartorio (criar stack separada) | `docker stack ls` | stacks segregadas |

## Squad S2 — LGPD & Segurança (cartorio-lgpd) · T051–T075

| T | Comando / ação | Validação | Evidência |
|---|----------------|-----------|-----------|
| T051 | Revisão LGPD obrigatória antes do merge de qualquer mudança em `pii*` ou `audit*` | branch `cartorio-lgpd` review | PR sign-off |
| T052 | Testes para `validate_cns`, `validate_cnh`, `validate_pis` com inputs malformados | `pytest tests/test_pii_validators.py` | 100% PASS |
| T053 | Garantir truncamento IPv4 em output (`pii.py`) | `pytest tests/test_pii_ip.py` | regra /24 |
| T054 | `audit_log` retém HMAC + SHA256 chain + dead-man-switch | `make audit-verify` | chain_ok=true |
| T055 | DPA MiniMax assinado? Atualizar tabela se não | `grep -i DPA docs/LGPD.md` | `LGPD_DPA_MINIMAX_SIGNED=true` |
| T056 | Revisão LGPD-015 (output scrub) | testes | 35/35 PII tests |
| T057 | Atualizar RIPD v1.2 com mudanças de 2026-07 | `docs/LGPD.md` atualizado | cabeçalho `2026-07-14` |
| T058 | Implementar `retention_days` por categoria de dado (PII vs. protocolo) | `pytest tests/test_lgpd_retencao_t036_t037.py` | verde |
| T059 | Garantir criptografia em repouso para backups S3 | `scripts/backup_v2.py` flags | AES-256 |
| T060 | Implementar `right to be forgotten` cascade (cliente + conversa + protocolo) | `pytest tests/test_lgpd_direito_esquecimento.py` | verde |
| T061 | Publicar política de privacidade no `/lgpd` | `curl https://api.2notasudi.com.br/lgpd` | 200 + HTML |
| T062 | Habilitar `LGPD_DPA_MINIMAX_SIGNED=true` no `.env` da VPS (após assinatura real) | `docker service inspect cartorio_api` | true |
| T063 | Garantir `RFC 7807` problem+json em todos os 4xx/5xx | `pytest tests/test_problem_details.py` | 100% |
| T064 | Adicionar `/api/v1/admin/audit/verify-now` para DPO | `curl POST` | 200 + chain_ok |
| T065 | Mascarar `client_ip` em logs (apenas `/24`) | `grep client_ip logs/*.log` | apenas truncado |
| T066 | Implementar `consent_grant_ip` (registrar IP do consentimento) | testes | coluna `lgpd_consent_ip` |
| T067 | Implementar `DPO nominal + email` (já existe `DPO_EMAIL`) | `.env` | `dpo@2notasudi.com.br` |
| T068 | Sanitizar logs Cloudflare: `x-forwarded-for` em logs do app | `pytest tests/test_log_masker.py` | mascarado |
| T069 | Garantir `data retention` de `outbox_message` (DLQ) por 90d | job `backend/app/jobs/retencao.py` | pytest |
| T070 | Validar HTTPS-only cookies em `/lgpd` | `pytest tests/test_cookies.py` | `Secure` flag |
| T071 | Testes para LGPD Art. 18 (acesso, correção, anonimização, portabilidade, eliminação, oposição, não-automação) | `pytest tests/test_lgpd_direitos_v2.py` | 7/7 |
| T072 | Garantir que respostas da API não vazem `cpf_hash` | `pytest tests/test_pii_hash.py` | mascarado |
| T073 | Implementar `audit` em `DELETE` (LGPD art. 18 VI) | tests | log gerado |
| T074 | Validar rotação de HMAC key (Lesson 25) | `pytest -m t025` | 100% |
| T075 | Atualizar `docs/LGPD.md` com cronograma 5y/2y | `cat docs/LGPD.md` | tabela atualizada |

## Squad S3 — Plataformas (cartorio-sre) · T076–T100

| T | Comando / ação | Validação | Evidência |
|---|----------------|-----------|-----------|
| T076 | Reinstalar Tailscale (Gustavo) | `tailscale status` | mostra nós |
| T077 | `ufw` allowlist portas: 80,443,22, e 2222 Tailscale | `ufw status` | 4 regras |
| T078 | Configurar Traefik path-based para Tailscale (`vps-cartorio.tail2fe279.ts.net`) | `curl https://vps-cartorio.tail2fe279.ts.net/api/v1/health/radar` | 200 |
| T079 | Provisionar Traefik rate-limit middleware (`cartorio-rate-limit`) | `curl -I` | 429 com X-RateLimit-* |
| T080 | Setup Cloudflare WAF rule (managed challenge) | dashboard CF | regra ativa |
| T081 | Configurar Uptime Kuma 5 serviços | dashboard Uptime | 5 monitores |
| T082 | Provisionar Prometheus + Grafana stack | `curl prometheus:9090` | 200 |
| T083 | Adicionar alerta `cartorio_api_5xx_rate > 1%` em 5min | Grafana alert | 1 alerta teste |
| T084 | Backup S3 (cartorio-backup) rodar 03:00 BRT | `cat /var/log/cartorio-backup-status.json` | ok |
| T085 | Validar tamanho do backup < 2GB | `ls -lh /var/backups/cartorio` | < 2GB |
| T086 | Testar restore de backup S3 → staging | `make restore-test` | 1 protocolo criado |
| T087 | Configurar auto-rotate de segredos (90d) | `scripts/rotate-secrets.sh` | execução OK |
| T088 | Provisionar OpenTelemetry collector na VPS | `/var/log/otel-collector.log` | OK |
| T089 | Integrar Langfuse tracing em OpenClaw | `langfuse.2notasudi.com.br` | 200 |
| T090 | Integrar Argilla feedback (cartorio-n8n) | `argilla.2notasudi.com.br` | 200 |
| T091 | Hardening Redis: `maxmemory-policy allkeys-lru` | `redis-cli CONFIG GET maxmemory-policy` | allkeys-lru |
| T092 | Redis: bind `127.0.0.1` (já está atrás de Tailscale) | `ss -tlnp` | bind interno |
| T093 | Habilitar `pg_stat_statements` no Postgres | `psql -c "SHOW shared_preload_libraries"` | pg_stat_statements |
| T094 | Cron `cartorio-evo-health` 5min (já existe) | `systemctl list-timers` | active |
| T095 | Documentar `Incident Response` runbook | `docs/INCIDENT_RESPONSE.md` | 5 cenários |
| T096 | Tabelar `SLO definitions` | `infra/slo/slo-definitions.yml` | 4 SLOs |
| T097 | Testar failover Redis (kill + recovery) | `docker kill cartorio_redis.1` | 30s recovery |
| T098 | Documentar deploy flow (Easypanel + manual) | `docs/DEPLOYMENT.md` | atualizado |
| T099 | Provisionar PagerDuty (cartorio-sre) integration | `pd.2notasudi.com.br` | 200 |
| T100 | Documentar DR plan (RTO 1h, RPO 15min) | `docs/DR_PLAN.md` | OK |

## Metas Globais

- **G1** — Integrar 100% serviços ao Postgres real (cartorio_supabase) e zerar `password authentication failed`.
- **G2** — Traefik + DNS para os 4 subdomínios faltantes.
- **G3** — LGPD-by-design com 100% PII scrubbing + audit chain intacto.
- **G4** — Documentação sincronizada (API, plataformas, ADRs).
- **G5** — Observabilidade fim-a-fim (Prometheus + Grafana + alerts).
- **G6** — Squads A–J, BRAIN e DOCS 100% no Harness (`459 → 459 done`).
- **G7** — CI/CD gates verdes em master (`make qa` 0 erros).
- **G8** — Integração total Cartório ↔ OpenClaw ↔ N8N ↔ Chatwoot ↔ Evolution ↔ Telegram.

## Squads paralelos (4 papéis)

- **S0 — Backend (cartorio-dev)**: Gustavo Almeida (CEO + tech-lead)
- **S1 — Multi-canal (cartorio-n8n)**: Pietra root (orquestrador n8n)
- **S2 — LGPD (cartorio-lgpd)**: Mavis (revisor LGPD)
- **S3 — Plataformas (cartorio-sre)**: SRE root

> Cada papel = 25 tarefas. Cada squad roda 1 sub-agente por vez, 2 max paralelo.
> Próximo ciclo (T101–T125): rebaixar gaps remanescentes e introduzir re-validations.
