## YYYY-MM-DD - Initial Setup
**Learning:** Initial Palette journal creation.
**Action:** Always maintain focus and accessibility improvements.
## 2024-09-13 - Add aria-live to dynamic AI result containers
**Learning:** Dynamically populated result containers, especially in AI extractions where content appears asynchronously after a button click, need `aria-live="polite"` so screen readers announce the changes to users.
**Action:** Always ensure dynamically populated result containers include `aria-live="polite"` for screen reader accessibility.
