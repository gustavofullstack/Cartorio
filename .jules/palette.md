## 2024-05-18 - Missing keyboard focus states\n**Learning:** The dashboards apply `outline: none` on `:hover, :focus-visible` combined styles to hide focus rings on mouse interactions, but this removes focus indicators for keyboard navigation.\n**Action:** Add a separate `:focus-visible` rule to buttons with `outline: 2px solid var(--brand)` to ensure keyboard accessibility while maintaining clean mouse interactions.
