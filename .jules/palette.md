## 2024-09-18 - Dynamically Populated Result Containers
**Learning:** Dynamically populated result containers (like AI extraction outputs) require screen reader accessibility so users are notified of changes.
**Action:** Always include `aria-live="polite"` on containers that update dynamically without page reloads.
