# N8N Audit 2026-06-23 — Auditoria Completa Pré-Sprint 3

**Data:** 2026-06-23 18:36-18:40 BRT
**Autor:** cartorio-n8n (mvs_b3f037cf485a4e21b899476eacaceff2)
**Trigger:** Consulta do cartorio-lgpd sobre gap LGPD P0.1 (signal `pii_blocked`/`needs_human_handoff`/`handoff_reason` não propaga do backend para N8N/compliance).
**Status:** Auditoria read-only COMPLETA. Sem commit. HOLD ativo até Gustavo autorizar formalmente.
**Master real:** `dff1bb9` (4 commits à frente de `d030e9c` citado por cartorio-dev).

---

## TL;DR

1. **15 workflows ativos no N8N** com **22 nodes `n8n-nodes-base.httpRequest`**.
2. **~17 nodes MANTER** (sem node oficial equivalente); **~5 SUBSTITUIR** (3 Alerta Chatwoot + WF #03 + WF #12 MCP). Esforço estimado migração: **~5h30** quando autorizado.
3. **MCP server `kTZUoh8ejvGxT8m9` está VAZIO** — só trigger, sem tools registradas. Deploy incompleto. Pré-requisito para WF #12 usar MCP: PRD do MCP server (tools, auth, transport).
4. **Credencial `chatwoot-api` está MAL CADASTRADA** — tipo `httpHeaderAuth` genérica, SEM campo `url`. Bloqueador real para migrar WF #03/#08/#09/#11 para node oficial `n8n-nodes-chatwoot`.
5. **WF #11 ativa usa URL TYPO** `chat.2notasudi.com.br` (1 voto) em vez de `chatwoot.2notasudi.com.br` (3 votos em outros WFs ativos).
6. **WF #00 Error Handler v4 tem node com nome errado**: `Alerta Chatwoot` aponta URL do backend `/atendimento`, não Chatwoot. Bug cosmético.
7. **WF #12 ativa = `WuQAi2ttarGGdPyD`** ("12 - Chatbot LLM End-to-End (PII + OpenCode-Go)"), usa httpRequest direto para `api/integrations/opencode/test`. **NÃO usa MCP** (MCP server está vazio). Arquivo local `infra/n8n-workflows/12-chatbot-llm-mcp.json` é **STAGING nunca deployed** (id=null).
8. **WF #03 ativa = `OQRIOVHcOjpkQ0Of`**, usa httpRequest com URL hardcoded `chatwoot.2notasudi.com.br`. Arquivo local `03-handoff-human-chatwoot.json` (com node oficial) também é STAGING nunca deployed.

---

## Deliverable A — Auditoria `n8n-nodes-base.httpRequest`

**Fonte:** `n8n export:workflow --all --pretty` em 2026-06-23 18:33 BRT.

### A.1 — WFs ativos (15 total, 22 nodes httpRequest)

| WF id | Nome | httpRequest count | Nodes | Method | URL | Substituir? | Node oficial | Deps | Esforço |
|-------|------|-------------------|-------|--------|-----|-------------|--------------|------|---------|
| `bR7qIo3bFpG4zgxO` | 01 Consulta Emolumento WhatsApp v3 | 2 | API Emolumento, Audit Log | GET, POST | `api/emolumento/calcular`, `audit/verify/log` | NÃO | — | — | 0 |
| `MzeYTSDouymzdpRw` | 02 Criar Protocolo LGPD | 1 | POST /protocolo | POST | `api/protocolo` | NÃO | — | — | 0 |
| `OQRIOVHcOjpkQ0Ok` | 03 Handoff Humano Chatwoot | 1 | Chatwoot Conversation | POST | `chatwoot.2notasudi.com.br/api/v1/.../conversations` | **SIM** | `n8n-nodes-chatwoot.Chatwoot` | SUI #1.7 (credencial) | 1h |
| `iXWuZRYZLR3FYPYB` | 04 Consulta Protocolo | 1 | GET /protocolo/{id} | GET | `api/protocolo/{id}` | NÃO | — | — | 0 |
| `UUW8ulDTxZUqBsci` | 05 Agendamento | 1 | API Disponibilidade | ? | `api/agendamento/disponibilidade` | NÃO | — | — | 0 |
| `ukbRUEudoX3SvsqD` | 06 Segunda Via | 1 | POST /documento/segunda-via | POST | `api/documento/segunda-via` | NÃO | — | — | 0 |
| `D9XJmlJRXZ3lavoa` | 07 Pesquisa Satisfacao | 1 | GET atendimentos | ? | `api/atendimentos/ultimas-24h` | NÃO | (Evolution node já usado p/ send) | — | 0 |
| `3rr2WFBCJZ16U4DH` | 08 Audit Verify Diario | 3 | POST /audit/verify, Alerta Chatwoot, Log OK | POST | `audit/verify`, `chatwoot.2notasudi.com.br/...`, `audit/verify/log` | **SIM** (1 de 3) | `n8n-nodes-chatwoot.Chatwoot` (Alerta) | SUI #1.7 | 30min |
| `pgtlDqGaMW1MGawt` | 09 Monitor Backup | 2 | GET /health/backup, Alerta Chatwoot | GET, POST | `health/backup`, `chatwoot.2notasudi.com.br/...` | **SIM** (1 de 2) | `n8n-nodes-chatwoot.Chatwoot` (Alerta) | SUI #1.7 | 30min |
| `5ABAZCQVRLd7AmM5` | 11 Monitor Cartorio | 7 | 6 GET health checks + Send Chatwoot Alert | GET, POST | `api/health`, `whatsapp.../`, `agent.../`, `supbase.../`, `chat.2notasudi.com.br` (TYPO!) | **SIM** (Alerta) + corrigir typo | `n8n-nodes-chatwoot.Chatwoot` | SUI #1.7 | 1h |
| `4IS5oiLyHWGhtb8g` | 00 Error Handler Global v4 | 1 | Alerta Chatwoot (nome errado!) | POST | `api/atendimento` (BUG: backend, não Chatwoot) | NÃO (renomear node) | — | — | 5min |
| `WuQAi2ttarGGdPyD` | 12 Chatbot LLM End-to-End OpenCode-Go | 1 | HTTP: OpenCode-Go | POST | `api/integrations/opencode/test` | **SIM** (longo prazo) | `n8n-nodes-mcp` (MCP `cartorio_chatbot_responder`) | PRD MCP + popular server | 2h |

### A.2 — WFs inativos (3, 6 nodes httpRequest — ignorar até promoção)

| WF id | Nome | httpRequest count | Status |
|-------|------|-------------------|--------|
| `8mY2uxWByRbYeJdn` | 12 Chatbot LLM ENHANCED (PII + Redis + Supabase) | 1 | INATIVO |
| `miw6DxlWJKS0NZOv` | 11 Monitor Cartorio ENHANCED (Radar + Supabase + Telegram) | 3 | INATIVO |
| `9vFJxUR1vHGe4w6q` | 13 Auto Cleanup & Maintenance | 2 | INATIVO |

### A.3 — Substituições possíveis (community packages já instaladas no container)

Inspeção de `/home/node/.n8n/nodes/node_modules/` confirmou:

| Package | Versão | Já usado em WFs ativos? |
|---------|--------|--------------------------|
| `@devlikeapro/n8n-nodes-chatwoot` | (latest) | **NENHUM** — bloqueador SUI #1.7 |
| `@winth03/n8n-nodes-minio` | (latest) | NENHUM |
| `n8n-nodes-evolution-api` | (latest) | WF #07 (Pesquisa Satisfacao) |
| `n8n-nodes-mcp` | v0.1.37 | **NENHUM** — MCP server vazio |
| `n8n-nodes-pdfkit` | (latest) | NENHUM |

### A.4 — Esforço total estimado de migração

- WF #03 (#1 prioridade — alta frequência): 1h
- WF #08 Alerta Chatwoot: 30min
- WF #09 Alerta Chatwoot: 30min
- WF #11 Alerta Chatwoot + corrigir typo URL: 1h
- WF #12 OpenCode-Go → MCP (depende PRD MCP + popular server): 2h
- WF #00 renomear node `Alerta Chatwoot` → `POST Atendimento Backend`: 5min

**Total: ~5h30 cartorio-n8n** (quando Gustavo der GO + deps resolvidas).

---

## Deliverable B — MCP Server `kTZUoh8ejvGxT8m9` VAZIO

### B.1 — Estado atual

- `active: true`, `isArchived: false`
- **1 único node**: `MCP Server Trigger` (`type: @n8n/n8n-nodes-langchain.mcpTrigger`, `typeVersion: 1`)
- `connections: {}` (zero)
- `webhookId: 67260601-4c9b-432f-9f97-f653ef1c51c8`
- `path: mcp-cartorio`

### B.2 — Por que vazio? Investigação

**3 hipóteses:**
1. **DEPLOY INCOMPLETO** (mais provável) — trigger foi deployado mas tools nunca foram adicionadas. WF #12 ativa contorna com httpRequest direto.
2. DESIGN INTENCIONAL — tools vêm do backend via MCP stdio transport (improvável, pattern seria diferente).
3. WORKAROUND DEPLOY — WF #12 ativa não usa MCP porque servidor está vazio; usa httpRequest direto para OpenCode-Go backend wrapper.

**Evidência:**
- WF #12 ativa `WuQAi2ttarGGdPyD` usa httpRequest `POST api/integrations/opencode/test`.
- Arquivo local `12-chatbot-llm-mcp.json` (que referencia `n8n-nodes-mcp.mcpClient` chamando `cartorio_chatbot_responder`) é **STAGING nunca deployed** (id=null).
- Curl test no path `https://n8n.2notasudi.com.br/webhook/mcp-cartorio` retornou HTTP 000 (DNS NXDOMAIN — subdomínio não configurado publicamente, esperado).

### B.3 — Pré-requisitos para popular MCP server

**Antes de implementar:** PRD do MCP server precisa ser definido (tools, auth, transport). SEM PRD = implementação cega.

Tools candidatas identificadas (a confirmar):
- `cartorio_chatbot_responder` (citado no WF mcp.json local)
- `protocolo_query`
- `emolumento_calc`
- `atendimento_create`
- `pii_scrub` (se exposto como tool)

Auth: bearer token (já existe credencial `cartorio-api-bearer` `22Q8OUbeZ1bsGnlt`).
Transport: HTTP streamable (MCP 2025-03-26 protocol).

---

## Deliverable C — Chatwoot typo + credencial mal cadastrada

### C.1 — Estado atual em PROD (15 WFs ativos)

| URL | WFs ativos | Status |
|-----|------------|--------|
| `chatwoot.2notasudi.com.br` | 3 (#03, #08, #09) | **CANÔNICO** (3 votos) |
| `chat.2notasudi.com.br` | 1 (#11) | **TYPO** (1 voto) |

### C.2 — Credencial `chatwoot-api` (`qGyW9nc36pWXo7ow`)

**Existe no N8N mas:**
- Tipo cadastrado: `httpHeaderAuth` (genérica)
- **NÃO tem campo `url`** (campo obrigatório do node `n8n-nodes-chatwoot`)
- **Usada por 0 WFs ativos** (só WF #11-ENHANCED INATIVA referencia)
- Decrypted data: `{name: "api_access_token", value: "={{$env.CHATWOOT_API_KEY}}"}`

**Bloqueador real:** se WF #03 migrar para `n8n-nodes-chatwoot.Chatwoot` AGORA, falha 100% porque node exige campo `url` na credencial.

**Fix necessário (SUI #1.7 — Gustavo):**
1. Recadastrar credencial como tipo `ChatWootApi` (do node `@devlikeapro/n8n-nodes-chatwoot`)
2. Campo `url` = `https://chatwoot.2notasudi.com.br`
3. Campo `accessToken` = `{{$env.CHATWOOT_API_KEY}}` (já existe)
4. Marcar como global (todas as credenciais Chatwoot usam o mesmo secret)

### C.3 — DNS check (Cloudflare externo)

| Host | IP | Status |
|------|----|--------|
| `api.2notasudi.com.br` | 187.77.236.77 | **RESOLVE** |
| `chatwoot.2notasudi.com.br` | — | **NXDOMAIN** (esperado, DNS pendente SUI #1.1) |
| `chat.2notasudi.com.br` | — | **NXDOMAIN** |

**Nota interna:** DNS Docker Swarm resolve via service name (roteamento interno Easypanel/Traefik). Os NXDOMAINs externos não impedem WF #03 ativo de funcionar internamente.

### C.4 — Ações dependentes de SUI #1.7

Após Gustavo recadastrar credencial:
- WF #03 → migrar para `n8n-nodes-chatwoot.Chatwoot` (1h)
- WF #08 Alerta Chatwoot → migrar (30min)
- WF #09 Alerta Chatwoot → migrar (30min)
- WF #11 → corrigir typo `chat.` → `chatwoot.` e usar credencial (1h)
- WF #00 → renomear node `Alerta Chatwoot` → `POST Atendimento Backend` (5min, não bloqueado)

---

## Insights extras (não estavam no briefing original)

### I.1 — WF #12 ativa ≠ arquivo local `12-chatbot-llm-mcp.json`

- **WF #12 ativa no N8N** = `WuQAi2ttarGGdPyD` ("12 - Chatbot LLM End-to-End (PII + OpenCode-Go)")
  - Usa `n8n-nodes-base.httpRequest` → `POST https://api.2notasudi.com.br/api/v1/integrations/opencode/test`
  - NÃO usa MCP
  - Tem `pii_blocked`, `needs_human_handoff`, `pii_redaction_count` no response shape (linhas 119-138 do arquivo local `12-chatbot-llm-end-to-end.json`)
- **Arquivo local** `infra/n8n-workflows/12-chatbot-llm-mcp.json` é **STAGING** (id=null) — NUNCA deployed
- Drift `'placa'` → `'placa_veiculo'` está no **arquivo staging**, não no WF prod
- WF #12 ENHANCED INATIVA (`8mY2uxWByRbYeJdn`) também tem id=null no arquivo local

### I.2 — WF #03 ativa ≠ arquivo local `03-handoff-human-chatwoot.json`

- **WF #03 ativa no N8N** = `OQRIOVHcOjpkQ0Ok` ("03 - Handoff Humano (Chatwoot)")
  - Usa `n8n-nodes-base.httpRequest` com URL hardcoded
  - NÃO usa `n8n-nodes-chatwoot.Chatwoot` (mesmo node oficial instalado)
- **Arquivo local** `infra/n8n-workflows/03-handoff-human-chatwoot.json` é **STAGING** (id=null) — NUNCA deployed

### I.3 — `infra/n8n-workflows/*.json` são todos STAGING

**Regra de auditoria:** todos os 25 arquivos em `infra/n8n-workflows/` têm `id: null` no payload. Significa que são TEMPLATES/STRUCTURES não deployed. **O prod é o export via `n8n export:workflow --all`.**

### I.4 — Master real vs referência do cartorio-dev

```
$ git log --oneline -5 master
dff1bb9 docs: document PII signal flow and architectural gaps between N8N and backend services in MEMORY.md
532ca93 refactor: update emolument calculation test expectations and document architecture/testing lessons in memory logs
5d914ba feat: add protocol tracking and fee calculator skills to OpenClaw, and document N8N/LGPD audit patterns.
a8581fe test: E1.S1.T7 final - PII zero leak via webhook Evolution (9/9 tests)
d030e9c test: fix E2E webhook test by correctly isolating app engine and bypassing lifespan events   ← referência do cartorio-dev, NÃO master mais
```

**Regra:** SEMPRE `git log --oneline -5 master` antes de comitar. `d030e9c` NÃO é master.

### I.5 — Changes não-commitadas em staging local

```
$ git status
modified:   .harness/TASKS.md
modified:   .harness/reins/cartorio-n8n/crons/n8n-quota-reset-check.md
modified:   backend/app/api/v1/integrations.py
modified:   backend/app/api/v1/router.py
```

Mudanças em `router.py` e `integrations.py` provavelmente são **plano A do cartorio-dev** (fix `router.py:631` para reincluir `pii_blocked`/`needs_human_handoff`/`handoff_reason`). Aguardando merge em master.

### I.6 — WF #00 Error Handler v4: bug cosmético

Node chamado `Alerta Chatwoot` aponta URL `https://api.2notasudi.com.br/api/v1/atendimento` — isso é **backend**, não Chatwoot. Nome enganoso. Renomear para `POST Atendimento Backend`.

---

## Sequência de execução proposta (quando Gustavo der GO)

```
HOJE (~19:30-20:00 BRT pós-reset):
  [ROOT] Aguardar Gustavo autorizar formalmente

GO DADO:
  1. SUI #1.7  Recadastrar credencial chatwoot-api (Gustavo via Easypanel/N8N UI) — 10min
  2. cartorio-dev A  Fix router.py:631 (reincluir pii_blocked/needs_human_handoff/handoff_reason) — 30min
                ↓ merge em master dff1bb9+
  3. cartorio-n8n G  Drift 'placa' → 'placa_veiculo' em 12-chatbot-llm-end-to-end.json (WF prod) — 30min
  4. cartorio-dev C  Publish em cartorio:atendimentos no criar_atendimento — 1h
  5. cartorio-dev B  Chamar criar_atendimento no PII path do router.py:503 — 2h
                ↓ backend publica eventos
  6. cartorio-n8n D  WF #25 Compliance PII Monitor (escuta cartorio:atendimentos) — 3h
  7. cartorio-lgpd E /api/v1/compliance/pii-stats — 1h
  8. cartorio-n8n WF #03 migração n8n-nodes-chatwoot — 1h (deps: SUI #1.7)
  9. cartorio-n8n WF #08, #09 Alerta Chatwoot migração — 1h (deps: SUI #1.7)
  10. cartorio-n8n WF #11 corrigir typo + usar credencial — 1h (deps: SUI #1.7)
  11. cartorio-n8n WF #00 renomear node bug cosmético — 5min
  12. (FUTURO) WF #12 OpenCode-Go → MCP (deps: PRD MCP + popular server) — 2h

Total: ~13h cartorio-n8n + ~3h30 cartorio-dev + ~1h cartorio-lgpd + ~10min Gustavo UI
```

---

## Bloqueadores identificados

| ID | Descrição | Owner | Status |
|----|-----------|-------|--------|
| SUI #1.1 | DNS `chatwoot.2notasudi.com.br` em Easypanel | Gustavo | Pendente |
| **SUI #1.7** | **Recadastrar credencial `chatwoot-api` como tipo ChatWootApi com url** | **Gustavo** | **Pendente (BLOQUEADOR)** |
| PRD MCP | PRD do MCP server (tools, auth, transport) | Gustavo | Pendente |
| Plano A merge | router.py:631 fix mergeado em master | cartorio-dev | Em andamento |
| Plano C merge | criar_atendimento publica em cartorio:atendimentos | cartorio-dev | Aguardando A |

---

## Cross-references

- `.harness/TASKS.md` — task tree principal (atualizar com SUI #1.7 e sequência acima)
- `.harness/memory/MEMORY.md` — lições aprendidas do projeto
- `agent memory (cartorio-n8n)` — `Live N8N vs arquivos locais`, `Chatwoot typo + credential`, `PII signal flow N8N vs backend`
- `docs/superpowers/specs/2026-06-23-sprint-3-design.md` — Sprint 3 design (aprovação pendente Gustavo)
- `backend/app/api/v1/router.py:439-635` — webhook_evolution (gap P0.1 LGPD)
- `backend/app/api/v1/ws/atendimentos.py` — T19 WebSocket (canal `cartorio:atendimentos` ORFÃO)
- `infra/n8n-workflows/*.json` — STAGING (id=null), NÃO prod

---

## Comandos de referência (auditoria reproduzível)

```bash
# Export live N8N workflows
ssh cartorio "docker exec \$(docker ps --format '{{.Names}}' | grep '^cartorio_n8n\.' | head -1) n8n export:workflow --all --pretty --output=/tmp/n8n-workflows-live.json"
ssh cartorio "docker cp \$(docker ps --format '{{.Names}}' | grep '^cartorio_n8n\.' | head -1):/tmp/n8n-workflows-live.json /tmp/n8n-live.json"
scp cartorio:/tmp/n8n-live.json /tmp/n8n-live.json

# Workflows com httpRequest
jq -r '.[] | . as $wf | ($wf.nodes // [] | map(select(.type == "n8n-nodes-base.httpRequest"))) as $n | select(($n | length) > 0) | "\($wf.id) | \($wf.name) | count=\($n | length) | active=\($wf.active)"' /tmp/n8n-live.json

# Export credentials (sem decrypted — mantém secrets mascarados)
ssh cartorio "docker exec \$(docker ps --format '{{.Names}}' | grep '^cartorio_n8n\.' | head -1) n8n export:credentials --all --output=/tmp/n8n-creds.json"

# MCP server trigger export
ssh cartorio "docker exec \$(docker ps --format '{{.Names}}' | grep '^cartorio_n8n\.' | head -1) n8n export:workflow --id=kTZUoh8ejvGxT8m9 --pretty"

# DNS check externo
dig +short api.2notasudi.com.br
dig +short chatwoot.2notasudi.com.br  # esperado NXDOMAIN
dig +short chat.2notasudi.com.br  # TYPO, esperado NXDOMAIN

# Master real check
git log --oneline -5 master
```

---

**Cross-reference TASKS.md (pendente — adicionar):**

```markdown
## EPIC N8N-AUDIT-2026-06-23 (pre-Sprint 3)
- [x] **N8N-AUDIT.A** Auditoria completa httpRequest (15 WFs ativos, 22 nodes) — owner: cartorio-n8n — done 18:36 BRT
- [x] **N8N-AUDIT.B** MCP server kTZUoh8ejvGxT8m9 VAZIO — owner: cartorio-n8n — done 18:38 BRT
- [x] **N8N-AUDIT.C** Chatwoot typo + credencial mal cadastrada — owner: cartorio-n8n — done 18:40 BRT
- [ ] **SUI #1.7** Recadastrar credencial chatwoot-api (BLOQUEADOR migração #03/#08/#09/#11) — owner: Gustavo — Pendente
- [ ] **WF #25 Compliance PII Monitor** (depende plano C backend) — owner: cartorio-n8n — Aguardando GO
- [ ] **WF #12 drift PII label** ('placa' → 'placa_veiculo', depende plano A merge) — owner: cartorio-n8n — Aguardando GO
```

---

**Autor:** cartorio-n8n (mvs_b3f037cf485a4e21b899476eacaceff2)
**Modified by Gustavo Almeida**