# Session Checkpoint — 2026-06-24 14:37 BRT

## Estado de partida (verificado, briefing verificado)
- Branch: `master` ✓ up-to-date com origin (HEAD `26a135b`)
- Working tree: 2 memos modificados + 5 memos novos untracked + 5 services LGPD novos untracked
- Working tree LIMPO de código: nenhum service/router/test em estado pendente
- Tailscale: ON (5 nodes); SSH cartorio + tailscale prontos
- `.env.example` raiz: 88 vars (template produção)
- `.env` backend: vars de produção (Supabase, Evolution, Chatwoot, N8N, OpenClaw, Opencode-Go, Deepseek)
- `.secrets/`: opencode-go.env, render.env, linear.env, jules.env, telegram.env, n8n.env (vazio)
- Telegram bot `8859206262:AAH...` = `@CartorioBot` (TEST) → hardcoded em `backend/app/api/v1/telegram.py` docstring + doc OpenClaw
- Opencode-Go `sk-xcRwExjQ...` → `.secrets/opencode-go.env` (`OPENCODE_GO_API_KEY`)
- Deepseek-v4-flash → primary model no OpenClaw + Opencode-Go
- OpenClaw `OPENCLAW_THINKING_ENABLED=true` no env raiz (já configurado ontem — checar runtime)

## MCPs disponíveis
- Locais: NENHUM (sem `.mcp.json`). Único MCP server é o do próprio backend (`backend/mcp_server.py`, exposto em `https://cartorio-n8n.dfgdxq.easypanel.host/mcp-server/http`).
- iOS simulator, Android emulator, browser-automation, ios-dev, android-dev, kimi-webbridge, openai-docs, paperclip*, prompt-cartorio, redesim-helper, etc = skills/plugins locais (sem servidor remoto).
- Para health-checks de produção: usar **SSH + Tailscale** (chave `~/.ssh/id_ed25519_cartorio` / `id_tailscale`) contra VPS Hostinger via `cartorio@100.99.172.84` (tail2fe279.ts.net).
- LLMs externos via API: MiniMax-M2.7 / M2.7-highspeed (já no OpenClaw), Render (`rnd_sTv1...`), Linear (`lin_api_...`), Jules (`AQ.Ab8...`).

## Constraints operacionais firmes
- NÃO rotacionar chaves (regra absoluta Gustavo)
- 1-2 agents por vez (limite 5h/assinatura)
- JSON compact para tasks/planos (economizar tokens)
- Master-only (Jules worktree/branch será mergeado e branch deletado)
- Sempre comentar/documentar/memory-save (`.harness/reins/cartorio-dev/memory/`)

## Decisões já tomadas
- Orquestrador = este chat (ZCode + minimax-M3 endpoint)
- 1 subagent Explore para mapear (✓ feito)
- Próximos agentes: apenas 1 por turno, escopo cirúrgico

## Próximos passos (curto)
1. Salvar plano 100 tasks (JSON + MD) — próxima ação
2. Health-check dos 8 serviços (SSH direto, sem agent)
3. Salvar MEMORY do loop (este arquivo + .harness/memory/)
4. Iniciar Squad A (Supabase real) — 10 tasks

## Squads definidos (10 tasks cada = 100)
- **Squad A** Supabase real (tabelas, migrations, RPC, cron, webhooks, vault, queues, MCP, GraphQL)
- **Squad B** Evolution API (integração Chatwoot/N8N/API/Supabase/Redis, multi-instance, fail-safe)
- **Squad C** N8N workflows (testar todos 33+, ativar oficiais, fix Code nodes, error handler, MCP server, Telegram listener)
- **Squad D** LGPD compliance (D6-D25 — 6 direitos PF, DPA vendors, RIPD, retenção, opt-out bot)
- **Squad E** OpenClaw Agent (thinkings runtime, contexto 1M, deepseek-v4-flash config, persona, skills 7)
- **Squad F** Telegram bot (TEST cartório + bot pessoal) — tokens, webhook, comandos, retry
- **Squad G** API FastAPI (testes 100% endpoints, OpenAPI, Postman, métricas Prometheus, segurança)
- **Squad H** Chatwoot CRM (inbox evolution, handoff, automations, custom attributes, reports)
- **Squad I** Redis (camadas: cache, queue, lock, rate limit, sliding window, idempotência, métricas, DLQ/outbox)
- **Squad J** Infra/DevOps/CI-CD (Traefik, Tailscale, Easypanel, backup systemd, render, linear, jules, monitoring, health)
