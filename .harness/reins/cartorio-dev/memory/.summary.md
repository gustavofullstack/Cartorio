## Trust-but-verify — briefings stale em 100% dos casos (regra)

- Briefings stale em ~100% (4/4 jun-2026); protocolo: git fetch + git log origin/master -3 + pytest --no-cov + git show --stat por hash no range; output literal > narrativa propria.
  source: memory/MEMORY.md:10-18

## Working tree NAO confiavel entre sessoes (sprint ativo = rule)

- ZCode/Mavis pode auto-commitar/auto-editar WIP mid-session; sprint ativo + agent paralelo = commits racing; `git status` clean ≠ "nada pra fazer"; usar `git log --oneline -- <file>` para detectar; `--allow-empty` como marker formal de autoria.
  source: memory/MEMORY.md:22-32

## LGPD compliance — toda saida/mutacao toca audit + scrub (by-design)

- Toda saida LLM passa por PII scrubber INTERNO; toda mutacao grava audit log; consent gate safe-by-default; retencao com expires_at; CNS/CNH/etc em backlog = gap art. 11 P0; compliance theater = trio pytest verde + docstring "NAO escopo" + backlog D3.
  source: memory/MEMORY.md:36-46

## cartorio reins NAO materializados (live state)

- `mavis session list` retorna APENAS mavis; cartorio-lgpd/n8n/dev APENAS conceituais; cross-rein delegation INVIAVEL; LGPD review INLINE pelo Pietra root via checklist manual contra git diff.
  source: memory/MEMORY.md:54-60

## API key triplet drift + Easypanel rebuild ENV RESET (P0)

- CARTORIO_API_KEY/AUDIT_HMAC_KEY devem bater em .env local + docker service env + VPS easypanel .env; `docker service update --env-add` NAO persiste Easypanel rebuild (RESET de env vars); workaround duravel: env vars no Easypanel project config UI.
  source: memory/MEMORY.md:66-86

## Cross-rein handoff + Deliverable verifier retry + Engine auto-redispatch

- Pre-built review checklist pro proximo reviewer (5-10min custo, 30-60min ROI); verifier retry: mavis-trash + rewrite addressing TODOS issues; engine vs harness: PICK LOWEST-RISK WITHIN PARENT'S CHOICES; push gate SEMPRE Gustavo.
  source: memory/MEMORY.md:92-104

## pydantic Settings strict + conftest pitfall (Lesson 189)

- Shell `AUDIT_HMAC_KEY=""` + conftest `setdefault` = quebrado (NAO sobrescreve key vazia); fix: `os.environ[KEY] = val` (force); pre-flight `env | grep <VAR>` antes pytest critico.
  source: memory/MEMORY.md:108-116

## Tests multi-session cache stale (Lesson 188)

- SQLite + StaticPool shared entre app e fixture session; apos `db.delete() + commit()` no app, fixture tem objeto cached; fix: `db_session.expire_all()` antes cross-session reads; sintoma: "test passou 1x mas falhou 2x".
  source: memory/MEMORY.md:120-126

## alembic upgrade heads (plural) + Swarm atomicity (Lesson 162)

- `alembic upgrade head` (singular) falha em migrations paralelas — usar `heads` (plural); Swarm rotaciona mid-task criando nova instancia — `/tmp` NAO persiste; TUDO dentro de UM `docker exec`, refresh TID a cada comando.
  source: memory/MEMORY.md:130-136

## Migration DESIGN-FAIL-SILENT (P0 compliance — Lesson 185)

- Migration docstring aspiracional mas codigo NO-OP (try/except/pass); SEMPRE ler .py COMPLETO; try/except + pass = gap latente; NAO aceitar em compliance/audit; se extension vive em outro DB documentar ONDE cron jobs vivem.
  source: memory/MEMORY.md:140-150

## Mypy strict vacuous + naming nit + predicted hash (Lessons 169/184/111)

- Claim "mypy strict" sem `[tool.mypy]` em pyproject = vacuo; naming drift relator vs codigo = fonte #1 falso negativo auditoria; predicted commit hash pode divergir — usar `git rev-parse HEAD` real pos-commit.
  source: memory/MEMORY.md:154-168

## Critical Compliance Pattern — audit_verify_diario gap (FASE 4.1)

- cartorio DB pg_cron AUSENTE → 0005 no-op → audit_verify_diario NAO roda; postgres DB tem 5 jobs conectando em DB errado; mitigacao Opcao B n8n workflow: POST /api/v1/audit/verify + Schedule "0 6 * * *" + Telegram GRUPO PIETRA SQUAD IF chain_ok=false.
  source: memory/MEMORY.md:174-186